# 📱 Applications

This folder contains all deployable applications.

## Structure

```
apps/
├── web/        # Web dashboard (React + Vite + Tailwind)
├── mobile/     # Mobile app (React Native + Expo)
└── backend/    # API server (Python + FastAPI)
```

## Overview

| App | Purpose | Users |
|-----|---------|-------|
| `web` | Admin dashboards & management | Teachers, Parents, Principals |
| `mobile` | Homework submission & tracking | Students, Parents |
| `backend` | REST API, business logic | All apps consume this |

## Running Apps

Each app has its own README with specific instructions.
